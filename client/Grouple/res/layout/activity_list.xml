<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/currentFriendsContainer"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
	android:background="@color/background_color"
    android:padding="0dp" >
    
    <ScrollView
        android:id="@+id/currentFriendsScrollView"
        android:layout_width="fill_parent"
        android:background="@color/layout_background_color"
        android:layout_height="wrap_content"
        android:layout_margin="0dp"
        android:padding="0dp" >

        <LinearLayout
            android:id="@+id/currentFriendsLayout"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" />
    </ScrollView>

      <Button
          android:id="@+id/addNewButtonLiA"
          android:layout_width="match_parent"
          android:layout_height="50sp"
          android:layout_below="@+id/currentFriendsScrollView"
          android:background="@drawable/button_border"
          android:onClick="startInviteActivity"
          android:text="Invite Friend"
          android:textColor="@color/button_text_color"
          android:textSize="20sp" />
    
</RelativeLayout>
